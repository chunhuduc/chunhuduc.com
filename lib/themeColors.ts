/**
 * RGB triple for `backgroundColor` overlays in client components (e.g. header blend).
 * Keep in sync with `app/globals.css` `:root --background` (currently `#1b1f24`).
 */
export const BACKGROUND_RGB = [27, 31, 36] as const;
