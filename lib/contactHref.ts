/** `WorkTogetherSection` default id on the homepage */
export const CONTACT_SECTION_ID = "contact";

/** Prefer this over `mailto:` for public CTAs (homepage contact form). */
export const CONTACT_FORM_HREF = `/#${CONTACT_SECTION_ID}` as const;
